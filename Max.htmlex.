DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>PyPractice – Learn Python</title>
  <style>
    body { font-family: Arial, sans-serif; margin: 0; padding: 0; background: #f4f6f8; }
    header { background: #222; color: white; padding: 15px; text-align: center; }
    main { padding: 15px; }
    textarea { width: 100%; height: 180px; font-size: 14px; }
    button { padding: 10px 15px; margin-top: 10px; font-size: 14px; }
    #output { background: #000; color: #0f0; padding: 10px; margin-top: 10px; min-height: 100px; }
    .card { background: white; padding: 15px; border-radius: 8px; margin-bottom: 15px; }
  </style>
</head>
<body>
  <header>
    <h2>PyPractice</h2>
    <p>Practice Python Anywhere 📱</p>
  </header>

  <main>
    <div class="card">
      <h3>Try Python Code</h3>
      <textarea id="code">print("Hello, Python!")</textarea>
      <br />
      <button onclick="runPython()">Run Code ▶</button>
      <div id="output"></div>
    </div>

    <div class="card">
      <h3>Daily Challenge</h3>
      <p>👉 Print numbers from 1 to 5 using Python.</p>
    </div>

    <div class="card">
      <h3>Monetization Ideas</h3>
      <ul>
        <li>Google AdSense ads</li>
        <li>Premium challenges (₹99/month)</li>
        <li>Affiliate links for Python courses</li>
      </ul>
    </div>
  </main>

  <script src="https://cdn.jsdelivr.net/pyodide/v0.23.4/full/pyodide.js"></script>
  <script>
    let pyodideReady = loadPyodide();

    async function runPython() {
      const output = document.getElementById("output");
      output.textContent = "Running...";
      const pyodide = await pyodideReady;
      try {
        let code = document.getElementById("code").value;
        let result = await pyodide.runPythonAsync(code);
        output.textContent = result === undefined ? "Done" : result;
      } catch (err) {
        output.textContent = err;
      }
    }
  </script>
</body>
</html>
